<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/back"
        >

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" android:id="@+id/relativeLayout">
        <ImageView
                android:id="@+id/title"
                android:background="@drawable/title"
                android:layout_width="fill_parent" android:layout_height="80dp"
                />
        <Button
                android:id="@+id/voice_btn"
                android:background="@drawable/voice_on"
                android:layout_width="60dp" android:layout_height="60dp" android:layout_centerVertical="true"
                android:layout_alignParentEnd="true"/>
        <Button
                android:id="@+id/game_btn"
                android:background="@drawable/ongoing"
                android:layout_toLeftOf="@+id/voice_btn"
                android:layout_centerVertical="true"
                android:layout_width="60dp" android:layout_height="60dp"/>
        <Button
                android:id="@+id/restart_btn"
                android:background="@drawable/restart"
                android:layout_toLeftOf="@+id/game_btn"
                android:layout_centerVertical="true"
                android:layout_width="60dp" android:layout_height="60dp"/>
    </RelativeLayout>

    <TextView
            android:id = "@+id/time"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:text="@string/hello_world"
            android:layout_below="@+id/relativeLayout" android:layout_alignParentStart="true"/>

    <FrameLayout
            android:id="@+id/frame"
            android:layout_toEndOf="@+id/time"
            android:layout_width="600dp" android:layout_height="500dp"
            android:layout_below="@+id/time">

    <TableLayout
            android:id ="@+id/map"
            android:orientation="horizontal"
            android:layout_width ="600dp"
            android:layout_height = "500dp"
            android:layout_marginRight="20dp"
            >
    </TableLayout>

    <ImageView
            android:id = "@+id/image"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ball"
            android:clickable="true"
            />
    </FrameLayout>

</RelativeLayout>
